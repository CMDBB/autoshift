# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
# the symlink bench build creates so node_modules is served under /assets
autoshift/public/node_modules
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*

# Rust / Cargo
/target/
Cargo.lock

.coverage

# Claude local stuff
/.claude/*local*

# this file is both ignored and committed, so it is available but its changes are ignored
rule_scratchpad.py

# captured DataPackage snapshots for sandbox/playground.ipynb: real dev-site data,
# re-capture with `bench capture-datapackage` instead of committing
sandbox/snapshots/*.json
# ad-hoc dumps from the sandbox notebook — same reason
sandbox/*.json

# `dump-dev-data` output: real employees/leaves from a live site. Practice-specific
# configuration belongs in the private companion app, not here.
dev_data/